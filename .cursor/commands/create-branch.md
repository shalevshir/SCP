# create-branch
Validate that the code satisfies the full Definition of Done (if available). Confirm that:

all required documentation is written and updated
all unit/integration tests pass
code coverage meets the project’s threshold

If and only if all criteria are met, create a dedicated branch and generate clean, logically separated commits suitable for a pull request.